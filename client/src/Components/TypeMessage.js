import React from "react";

const TypeMessage = () => {
    return (
        <div>
            <input type="text" placeholder="Hey..." />
            <button>/</button>
        </div>
    );
}

export default TypeMessage;